import { describe, it } from "vitest";

describe("src/lib/native/current-word.ts", () => {
  it.todo("covers the primary behavior in current-word");
  it.todo("covers edge cases and regression paths in current-word");
});
