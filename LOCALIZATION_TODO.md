# Localization TODO

- Add an in-app language override so users can choose a preferred app language independently of the system language. Persist the choice locally, expose a clear system-default option, and verify App Store metadata/screenshot locales still map cleanly to the in-app language set across Apple platforms.
